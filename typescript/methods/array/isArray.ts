/*

O método Array.isArray() retorna true se um objeto é uma array, e false se não é.

*/

const notArray = { name: "Pedro", age: 13 };
const isArray = ["Pedro", 13];
const isArrayTeste1 = new Array(); // = []

//console.log(Array.isArray(notArray));
//console.log(Array.isArray(isArray));
//console.log(Array.isArray(isArrayTeste1)); // true
